<UserControl x:Class="WarehouseApp.Views.OrdersWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="Заказы магазинов" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text="Список заказов готовых к отгрузке" Margin="0,5,0,0"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Отправить выбранные" Command="{Binding ShipSelectedCommand}"
                        Background="#3498db" Foreground="White" Width="150"/>
            </StackPanel>
        </StackPanel>

        <!-- Основная область -->
        <Grid Grid.Row="1" Margin="10">

            <!-- Сообщение когда нет заказов -->
            <TextBlock Text="Нет заказов готовых к отгрузке. Заказы создаются каждый день автоматически." 
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       TextAlignment="Center" TextWrapping="Wrap"
                       FontSize="16" Foreground="Gray" Width="400">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding WrappedOrders.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Таблица когда есть заказы -->
            <DataGrid x:Name="OrdersGrid" 
                      ItemsSource="{Binding WrappedOrders}" 
                      AutoGenerateColumns="False"
                      SelectionMode="Extended" 
                      SelectionUnit="FullRow"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="False">
                <DataGrid.Style>
                    <Style TargetType="DataGrid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding WrappedOrders.Count}" Value="0">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Style>
                <DataGrid.Columns>
                    <!-- Колонка с чекбоксом -->
                    <DataGridTemplateColumn Header="Выбор" Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          IsHitTestVisible="True"
                                          Focusable="True"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Магазин" Binding="{Binding StoreId}" Width="70" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Товаров" Binding="{Binding ItemsCount}" Width="70" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Сумма" Width="100" IsReadOnly="True">
                        <DataGridTextColumn.Binding>
                            <Binding Path="TotalAmount" StringFormat="{}{0:N2} руб."/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Дата" 
                                       Binding="{Binding OrderDate, StringFormat=dd.MM.yyyy}" 
                                       Width="100" 
                                       IsReadOnly="True"/>
                    <DataGridTextColumn Header="Статус" 
                                       Binding="{Binding Status}" 
                                       Width="100"
                                       IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Статусная строка - упрощенная версия -->
        <Border Grid.Row="2" Background="#f8f9fa" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Выбрано заказов: " FontWeight="Bold"/>
                <TextBlock Text="{Binding ElementName=OrdersGrid, Path=SelectedItems.Count}"/>
                <TextBlock Text=" из " Margin="5,0,0,0"/>
                <TextBlock Text="{Binding WrappedOrders.Count}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>